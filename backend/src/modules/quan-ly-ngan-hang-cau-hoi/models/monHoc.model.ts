/**
 * Interface cho Môn Học (Học phần)
 */
export interface IMonHoc {
    id: string;
    maMonHoc: string;   // Mã môn (VD: "CS101")
    tenMonHoc: string;  // Tên môn
    soTinChi: number;   // Số tín chỉ
}
